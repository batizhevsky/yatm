<% publish_to "/messages/update" do %>
  var task = $(".task-id<%= @task.id %>");
  $(task).fadeOut("slow", function(){
    var new_task = $("<%= j render(@task) %>");
    $(this).replaceWith(new_task);
    $(task).fadeIn("slow");
  });
<% end %>
